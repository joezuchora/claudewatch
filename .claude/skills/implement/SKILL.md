# Implement Module

1. Read the spec file provided by the user
2. Implement all functions with full TypeScript types
3. Create corresponding .test.ts file with comprehensive tests
4. Run `bun test` and fix any failures
5. Run `bunx tsc --noEmit` to verify types
6. Commit with message: "feat: implement <module-name> per spec"
