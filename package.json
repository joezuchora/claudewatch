{
  "name": "claudewatch",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "install-statusline": "bun run --filter @claudewatch/statusline install-statusline"
  },
  "devDependencies": {
    "bun-types": "^1.3.10",
    "typescript": "^6.0.2"
  }
}
