{
  "name": "@claudewatch/metrics",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "serve": "bun run src/cli-serve.ts",
    "ship": "bun run src/cli-ship.ts",
    "build": "bun build src/cli-serve.ts --target node --outdir dist",
    "detect": "bun run src/cli-detect.ts"
  },
  "dependencies": {
    "@claudewatch/core": "workspace:*"
  }
}
